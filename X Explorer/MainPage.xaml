<Page x:Name="page"
    x:Class="X_Explorer.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:X_Explorer"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    xmlns:muxc="using:Microsoft.UI.Xaml.Controls"
     
    >
    <Page.Resources>
        <ResourceDictionary>
            <AcrylicBrush x:Key="NavigationViewTopPaneBackground"
                  BackgroundSource="Backdrop" FallbackColor="{ThemeResource SystemAltHighColor}" AlwaysUseFallback="True"/>

        </ResourceDictionary>
    </Page.Resources>


    <Grid x:Name="grid">
        <Button x:Name="AppButton" VerticalAlignment="Top" Width="70" Height="70" CornerRadius="90" Canvas.ZIndex="10" Background="White" Margin="10" Style="{StaticResource ButtonRevealStyle}">
            <Button.Shadow>
                <ThemeShadow/>
            </Button.Shadow>

            <FontIcon Glyph="&#xE81E;"/>

        </Button>
        <muxc:TabView x:Name="tabView" AddTabButtonClick="TabView_AddTabButtonClick" TabCloseRequested="TabView_TabCloseRequested" CanDragTabs="True" CloseButtonOverlayMode="OnPointerOver" Height="40">
            <muxc:TabView.Background>
                <AcrylicBrush TintColor="{ThemeResource SystemAccentColor}" FallbackColor="{ThemeResource SystemAccentColor}" BackgroundSource="HostBackdrop" TintOpacity="0.8"/>
            </muxc:TabView.Background>

            <muxc:TabView.TabStripHeader>
                <Grid x:Name="ShellTitlebarInset" Background="Transparent" Width="90"/>
            </muxc:TabView.TabStripHeader>

            <muxc:TabView.TabStripFooter>
                <Grid>
                    <Rectangle x:Name="CustomDragRegion" Margin="0,0,0,0" Fill="Transparent" HorizontalAlignment="Stretch"/>
                    <SearchBox BorderBrush="{x:Null}" Margin="0,0,150,0" Width="200" Background="{x:Null}" HorizontalAlignment="Right" SearchHistoryEnabled="False"/>
                    <TextBlock Text="Development Branch"/>
                </Grid>


            </muxc:TabView.TabStripFooter>
        </muxc:TabView>
        <NavigationView x:Name="NavigationViewTopPaneBackground" PaneDisplayMode="Top" IsBackButtonVisible="Collapsed" IsPaneToggleButtonVisible="False" IsSettingsVisible="False" OverflowLabelMode="MoreLabel" VerticalAlignment="Top" IsPaneOpen="False" Margin="0,40,0,0">
            <NavigationView.Resources>

            </NavigationView.Resources>
            <NavigationView.MenuItems>
                <NavigationViewItemHeader Width="90" HorizontalAlignment="Left"/>
                <NavigationViewItem Content="Home"/>
                <NavigationViewItem Content="Tools"/>
                <NavigationViewItem Content="View"/>
                <NavigationViewItem Content="Share"/>
                

            </NavigationView.MenuItems>
            <Frame Name="Ribbon" Height="150" Navigated="Ribbon_Navigated" Padding="20" Background="{ThemeResource ToggleButtonPressedForegroundThemeBrush}">
                <StackPanel Orientation="Horizontal">
                    <StackPanel Orientation="Vertical">
                        <Button Width="60" Height="100" HorizontalContentAlignment="Center" CornerRadius="1.5,1.5,1.5,1.5" Background="Transparent" Style="{StaticResource ButtonRevealStyle}">
                            <StackPanel Orientation="Vertical">
                                <SymbolIcon Symbol="Copy" />
                                <TextBlock FontSize="14" Margin="0,5,0,30" Text="Copy" />
                            </StackPanel>

                        </Button>

                    </StackPanel>
                    <StackPanel Orientation="Vertical">
                        <Button Width="60" Height="70" HorizontalContentAlignment="Center" CornerRadius="1.5,1.5,0,0" Background="Transparent" Style="{StaticResource ButtonRevealStyle}">
                            <StackPanel Orientation="Vertical">
                                <SymbolIcon Symbol="Paste" />
                                <TextBlock FontSize="14" Margin="0,5,0,0" Text="Paste" />
                            </StackPanel>

                        </Button>
                        <DropDownButton Width="60" Height="30" HorizontalContentAlignment="Center" CornerRadius="0,0,1.5,1.5" Background="Transparent" Style="{StaticResource ButtonRevealStyle}">
                            <DropDownButton.Content>
                                <FontIcon FontSize="10" FontFamily="{StaticResource SymbolThemeFontFamily}" Glyph="&#xE70D;" />
                            </DropDownButton.Content>


                            <DropDownButton.Flyout>
                                <MenuFlyout Placement="Bottom">
                                    <MenuFlyoutItem Text="Paste Text Only">
                                        <MenuFlyoutItem.Icon>
                                            <FontIcon FontFamily="Segoe MDL2 Assets" Glyph="&#xE725;" />
                                        </MenuFlyoutItem.Icon>
                                    </MenuFlyoutItem>
                                    <MenuFlyoutItem Text="Reply">
                                        <MenuFlyoutItem.Icon>
                                            <FontIcon FontFamily="Segoe MDL2 Assets" Glyph="&#xE8CA;" />
                                        </MenuFlyoutItem.Icon>
                                    </MenuFlyoutItem>
                                    <MenuFlyoutItem Text="Reply All">
                                        <MenuFlyoutItem.Icon>
                                            <FontIcon FontFamily="Segoe MDL2 Assets" Glyph="&#xE8C2;" />
                                        </MenuFlyoutItem.Icon>
                                    </MenuFlyoutItem>
                                </MenuFlyout>
                            </DropDownButton.Flyout>
                        </DropDownButton>
                    </StackPanel>
                    <StackPanel Orientation="Vertical">
                        <Button HorizontalContentAlignment="Left" CornerRadius="1.5,1.5,1.5,1.5" Background="Transparent" Style="{StaticResource ButtonRevealStyle}">
                            <StackPanel Orientation="Horizontal">
                                <SymbolIcon Symbol="Cut" />
                                <TextBlock FontSize="14" Text="Cut" Margin="5,0,0,0" />
                            </StackPanel>
                        </Button>
                        <Button HorizontalContentAlignment="Left" CornerRadius="1.5,1.5,1.5,1.5" Background="Transparent" Style="{StaticResource ButtonRevealStyle}">
                            <StackPanel Orientation="Horizontal">
                                <SymbolIcon Symbol="Copy" />
                                <TextBlock FontSize="14" Text="Copy Path" Margin="5,0,0,0" />
                            </StackPanel>
                        </Button>
                        <Button HorizontalContentAlignment="Left" CornerRadius="1.5,1.5,1.5,1.5" Background="Transparent" Style="{StaticResource ButtonRevealStyle}">
                            <StackPanel Orientation="Horizontal">
                                <SymbolIcon Symbol="Go" />
                                <TextBlock FontSize="14" Text="Paste Shortcut" Margin="5,0,0,0" />
                            </StackPanel>
                        </Button>
                    </StackPanel>
                </StackPanel>
            </Frame>
        </NavigationView>
        <NavigationView Name="TabViewItemCollection" IsPaneToggleButtonVisible="False" IsSettingsVisible="False" IsBackButtonVisible="Collapsed" HorizontalContentAlignment="Stretch" Margin="0,230,0,0" AllowDrop="True" PaneDisplayMode="Left">

        </NavigationView>
    </Grid>
</Page>
