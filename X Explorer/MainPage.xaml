<Page x:Name="page"
    x:Class="X_Explorer.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:X_Explorer"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:muxc="using:Microsoft.UI.Xaml.Controls"
    xmlns:controls="using:Microsoft.Toolkit.Uwp.UI.Controls"
    xmlns:core="using:Microsoft.Xaml.Interactions.Core"
    xmlns:interactivity="using:Microsoft.Xaml.Interactivity"
    mc:Ignorable="d"
    
    
    >
    <Page.Resources>
        <ResourceDictionary>
            <AcrylicBrush x:Key="NavigationViewTopPaneBackground" BackgroundSource="Backdrop" FallbackColor="{ThemeResource SystemAltHighColor}" AlwaysUseFallback="True"/>
            <AcrylicBrush x:Key="NavigationViewExpandedPaneBackground" BackgroundSource="HostBackdrop" TintColor="{ThemeResource SystemChromeHighColor}" TintOpacity=".8"/>
            <ThemeShadow x:Name="Shadow"/>
        </ResourceDictionary>

    </Page.Resources>

    <Grid x:Name="grid">
        <Grid x:Name="ShadowGrid" CornerRadius="90" Shadow="{StaticResource Shadow}" Translation="0,0,100" Width="  70" Height="70" HorizontalAlignment="Left" VerticalAlignment="Top" Canvas.ZIndex="100" Margin="10,10,0,0" Background="White">
            <Button x:Name="AppButton" VerticalAlignment="Top" Width="70" Height="70" CornerRadius="90" Canvas.ZIndex="10" Style="{StaticResource ButtonRevealStyle}" Background="White">
                <FontIcon Glyph="&#xE81E;"/>
            </Button>
        </Grid>

        <muxc:TabView x:Name="tabView" AddTabButtonClick="TabView_AddTabButtonClick" TabCloseRequested="TabView_TabCloseRequested" CanDragTabs="True" CloseButtonOverlayMode="OnPointerOver" Height="40" Background="{ThemeResource SystemControlAccentAcrylicWindowAccentMediumHighBrush}" AllowDrop="True">
            <muxc:TabView.TabStripHeader>
                <Grid x:Name="ShellTitlebarInset" Background="Transparent" Width="90"/>
            </muxc:TabView.TabStripHeader>

            <muxc:TabView.TabStripFooter>
                <Grid>
                    <Rectangle x:Name="CustomDragRegion" Margin="0,0,0,0" Fill="Transparent" HorizontalAlignment="Stretch"/>
                </Grid>


            </muxc:TabView.TabStripFooter>
        </muxc:TabView>
        <controls:DropShadowPanel BlurRadius="5.0" ShadowOpacity="1" OffsetX="0" OffsetY="5.0" Color="#DDDDDD" HorizontalAlignment="Stretch" VerticalAlignment="Top" Width="{Binding ActualWidth, ElementName=page, Mode=OneWay}" Margin="0,40,0,0" Canvas.ZIndex="1">
            <NavigationView x:Name="NavigationViewTopPaneBackground" PaneDisplayMode="Top" IsBackButtonVisible="Collapsed" IsPaneToggleButtonVisible="False" IsSettingsVisible="False" OverflowLabelMode="MoreLabel" VerticalAlignment="Top" IsPaneOpen="False" Width="{Binding ActualWidth, ElementName=page, Mode=OneWay}">

                <NavigationView.MenuItems>
                    <NavigationViewItemHeader Width="90" HorizontalAlignment="Left"/>
                    <NavigationViewItem Content="Home"/>
                    <NavigationViewItem Content="Tools"/>
                    <NavigationViewItem Content="View"/>
                    <NavigationViewItem Content="Share"/>
                </NavigationView.MenuItems>
                <Frame Name="Ribbon" Height="150" Navigated="Ribbon_Navigated" Padding="20" Background="{ThemeResource ToggleButtonPressedForegroundThemeBrush}">
                    <StackPanel Orientation="Horizontal">
                        <StackPanel Orientation="Vertical">
                            <Button Width="60" Height="100" HorizontalContentAlignment="Center" CornerRadius="1.5,1.5,1.5,1.5" Background="Transparent" Style="{StaticResource ButtonRevealStyle}">
                                <StackPanel Orientation="Vertical">
                                    <SymbolIcon Symbol="Copy" />
                                    <TextBlock FontSize="14" Margin="0,5,0,30" Text="Copy" />
                                </StackPanel>

                            </Button>

                        </StackPanel>
                        <StackPanel Orientation="Vertical">
                            <Button Width="60" Height="70" HorizontalContentAlignment="Center" CornerRadius="1.5,1.5,0,0" Background="Transparent" Style="{StaticResource ButtonRevealStyle}">
                                <StackPanel Orientation="Vertical">
                                    <SymbolIcon Symbol="Paste" />
                                    <TextBlock FontSize="14" Margin="0,5,0,0" Text="Paste" />
                                </StackPanel>

                            </Button>
                            <DropDownButton Width="60" Height="30" HorizontalContentAlignment="Center" CornerRadius="0,0,1.5,1.5" Background="Transparent" Style="{StaticResource ButtonRevealStyle}">
                                <DropDownButton.Content>
                                    <FontIcon FontSize="10" FontFamily="{StaticResource SymbolThemeFontFamily}" Glyph="&#xE70D;" />
                                </DropDownButton.Content>


                                <DropDownButton.Flyout>
                                    <MenuFlyout Placement="Bottom">
                                        <MenuFlyoutItem Text="Paste Text Only">
                                            <MenuFlyoutItem.Icon>
                                                <FontIcon FontFamily="Segoe MDL2 Assets" Glyph="&#xE725;" />
                                            </MenuFlyoutItem.Icon>
                                        </MenuFlyoutItem>
                                        <MenuFlyoutItem Text="Reply">
                                            <MenuFlyoutItem.Icon>
                                                <FontIcon FontFamily="Segoe MDL2 Assets" Glyph="&#xE8CA;" />
                                            </MenuFlyoutItem.Icon>
                                        </MenuFlyoutItem>
                                        <MenuFlyoutItem Text="Reply All">
                                            <MenuFlyoutItem.Icon>
                                                <FontIcon FontFamily="Segoe MDL2 Assets" Glyph="&#xE8C2;" />
                                            </MenuFlyoutItem.Icon>
                                        </MenuFlyoutItem>
                                    </MenuFlyout>
                                </DropDownButton.Flyout>
                            </DropDownButton>
                        </StackPanel>
                        <StackPanel Orientation="Vertical">
                            <Button HorizontalContentAlignment="Left" CornerRadius="1.5,1.5,1.5,1.5" Background="Transparent" Style="{StaticResource ButtonRevealStyle}">
                                <StackPanel Orientation="Horizontal">
                                    <SymbolIcon Symbol="Cut" />
                                    <TextBlock FontSize="14" Text="Cut" Margin="5,0,0,0" />
                                </StackPanel>
                            </Button>
                            <Button HorizontalContentAlignment="Left" CornerRadius="1.5,1.5,1.5,1.5" Background="Transparent" Style="{StaticResource ButtonRevealStyle}">
                                <StackPanel Orientation="Horizontal">
                                    <SymbolIcon Symbol="Copy" />
                                    <TextBlock FontSize="14" Text="Copy Path" Margin="5,0,0,0" />
                                </StackPanel>
                            </Button>
                            <Button HorizontalContentAlignment="Left" CornerRadius="1.5,1.5,1.5,1.5" Background="Transparent" Style="{StaticResource ButtonRevealStyle}">
                                <StackPanel Orientation="Horizontal">
                                    <SymbolIcon Symbol="Go" />
                                    <TextBlock FontSize="14" Text="Paste Shortcut" Margin="5,0,0,0" />
                                </StackPanel>
                            </Button>
                        </StackPanel>
                    </StackPanel>
                </Frame>
            </NavigationView>
        </controls:DropShadowPanel>
        <NavigationView Name="TabViewItemCollection" IsPaneToggleButtonVisible="False" IsSettingsVisible="False" IsBackButtonVisible="Collapsed" HorizontalContentAlignment="Stretch" Margin="0,232,0,0" AllowDrop="True" PaneDisplayMode="Left" VerticalAlignment="Stretch" Width="Auto">
            <NavigationView.MenuItems>
                <NavigationViewItemHeader Width="90" HorizontalAlignment="Left"/>
                <NavigationViewItem Content="Quick Access" CanDrag="True" Icon="OutlineStar"/>
                <NavigationViewItem Content="Desktop" CanDrag="True" Icon="OutlineStar"/>
                <NavigationViewItem Content="Documents" CanDrag="True" Icon="OutlineStar"/>
                <NavigationViewItem Content="Downloads" CanDrag="True" Icon="OutlineStar"/>
                <NavigationViewItem Content="OneDrive" CanDrag="True" Icon="OutlineStar"/>

            </NavigationView.MenuItems>
            <NavigationView.Content>
                <Grid x:Name="FolderContent">
                    <Grid x:Name="Pathbar" Height="40" VerticalAlignment="Top" HorizontalAlignment="Stretch">
                        <Button Width="40" Height="40" HorizontalContentAlignment="Center" CornerRadius="1.5,1.5,1.5,1.5" Background="Transparent" Style="{StaticResource ButtonRevealStyle}" VerticalAlignment="Top" HorizontalAlignment="Left">
                            <SymbolIcon Symbol="Back" />
                        </Button>
                        <Button Width="40" Height="40" HorizontalContentAlignment="Center" CornerRadius="1.5,1.5,1.5,1.5" Background="Transparent" Style="{StaticResource ButtonRevealStyle}" VerticalAlignment="Top" HorizontalAlignment="Left" Margin="40,0,0,0">
                            <SymbolIcon Symbol="Forward" />
                        </Button>
                        <TextBox HorizontalContentAlignment="Left" AcceptsReturn="True" AllowDrop="True" BorderBrush="{x:Null}" TextChanged="TextBox_TextChanged" PlaceholderText="Path" Height="40" Width="Auto" HorizontalAlignment="Stretch" Margin="80,0,40,0"/>
                        <Button Width="40" Height="40" HorizontalContentAlignment="Center" CornerRadius="1.5,1.5,1.5,1.5" Background="Transparent" Style="{StaticResource ButtonRevealStyle}" VerticalAlignment="Top" HorizontalAlignment="Right">
                            <SymbolIcon Symbol="Forward" />
                        </Button>
                    </Grid>
                    <Grid>
                        <GridView Margin="0,40,0,0">

                        </GridView>

                    </Grid>
                </Grid>
            </NavigationView.Content>
        </NavigationView>

    </Grid>
</Page>
